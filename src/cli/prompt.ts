/**
 * Compatibility facade that re-exports the CLI prompt runtime types and factory.
 */
export { createPromptRuntime } from "../interaction/prompt";
export type { CliPromptRuntime, PromptChoice } from "../interaction/prompt";
